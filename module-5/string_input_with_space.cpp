#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    // cin.getline(s,100); // for character array => s[100];
    cin.ignore // for not take enter.
    getline(cin,s); // for dynamic array or character array which is build with string class.
    return 0;
}